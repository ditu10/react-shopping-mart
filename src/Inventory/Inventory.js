import React from 'react';

const Inventory = () => {
    return (
        <div>
            <h2 className="text-4xl font-semibold text-yellow-400 italic">Inventory</h2>
        </div>
    );
};

export default Inventory;